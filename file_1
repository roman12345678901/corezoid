function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

var range = getRandomInt(10); // диапазон от 0 до 9

if ( range <= 0 ) // От от 0 до 4 = 50%, от 0 до 3 = 40%, от 0 до 2 = 30% и тд.
    {
        data.group = "CG";
    }
else data.group = "TG";
